image: docker:stable

variables:
  DOCKER_HOST: tcp://docker:2375/
  DOCKER_DRIVER: overlay2

services:
  - docker:dind

before_script:
   #   - sudo apt-get update -y && sudo apt-get install -y -qq docker-ce
   - docker --version 
   - docker info

build:
   stage: build
   script:
     - docker build -t my-docker-image .
#build_image:
#   script:
#     - docker build -t my-docker-image .
#     - docker run my-docker-image /script/to/run/tests
