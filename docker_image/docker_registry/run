docker -D run -d \
        --restart=always \
        --name daikuei_registry \
        -v ${PWD}/storage:/var/lib/registry \
        -v ${PWD}/private_reg_config.yml:/etc/docker/registry/config.yml \
        -p 5000:5000 \
        registry:2
